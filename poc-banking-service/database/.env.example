# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=poc_banking
DB_USER=postgres
DB_PASSWORD=postgres

# Connection Pool
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# SSL (set to true for production)
DB_SSL=false

# Flyway Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/poc_banking

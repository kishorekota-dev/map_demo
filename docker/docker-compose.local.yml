services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: poc_banking
      POSTGRES_USER: poc_user
      POSTGRES_PASSWORD: poc_password
      POSTGRES_MULTIPLE_DATABASES: poc_banking,poc_chat,ai_orchestrator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../scripts/init-multiple-databases.sh:/docker-entrypoint-initdb.d/01-init-multiple-databases.sh:ro
      - ../scripts/init-ai-orchestrator-db.sql:/docker-entrypoint-initdb.d/02-init-ai-orchestrator-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U poc_user -d poc_banking"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  banking-service:
    build:
      context: ../services/banking-service
    environment:
      NODE_ENV: development
      PORT: 3005
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: poc_banking
      DB_USER: poc_user
      DB_PASSWORD: poc_password
      DATABASE_URL: postgresql://poc_user:poc_password@postgres:5432/poc_banking
      JWT_SECRET: dev-jwt-secret-change-in-production
      ALLOWED_ORIGINS: http://localhost:3000,http://localhost:3006
      NLU_SERVICE_URL: http://nlu-service:3003
      NLP_SERVICE_URL: http://nlu-service:3003
      MCP_SERVICE_URL: http://mcp-service:3004
    ports:
      - "3005:3005"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3005/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  nlu-service:
    build:
      context: ../services/nlu-service
    environment:
      NODE_ENV: development
      NLU_SERVICE_PORT: 3003
      CORS_ORIGIN: http://localhost:3000,http://localhost:3006
      BANKING_SERVICE_URL: http://banking-service:3005
      NLP_SERVICE_URL: http://nlu-service:3003
      MCP_SERVICE_URL: http://mcp-service:3004
      DIALOGFLOW_ENABLED: ${DIALOGFLOW_ENABLED:-false}
      DIALOGFLOW_PROJECT_ID: ${DIALOGFLOW_PROJECT_ID:-}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/dialogflow-key.json
    ports:
      - "3003:3003"
    volumes:
      - ../services/nlu-service/credentials:/app/credentials:ro
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3003/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  mcp-service:
    build:
      context: ../services/mcp-service
    environment:
      NODE_ENV: development
      MCP_SERVICE_PORT: 3004
      BANKING_SERVICE_URL: http://banking-service:3005
      NLP_SERVICE_URL: http://nlu-service:3003
      NLU_SERVICE_URL: http://nlu-service:3003
    ports:
      - "3004:3004"
    depends_on:
      banking-service:
        condition: service_healthy
      nlu-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3004/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  ai-orchestrator:
    build:
      context: ../services/ai-orchestrator
    environment:
      NODE_ENV: development
      PORT: 3007
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ai_orchestrator
      DB_USER: poc_user
      DB_PASSWORD: poc_password
      JWT_SECRET: dev-jwt-secret-change-in-production
      OPENAI_ENABLED: "${OPENAI_ENABLED:-false}"
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4}
      SLM_ENABLED: "${SLM_ENABLED:-false}"
      SLM_BASE_URL: ${SLM_BASE_URL:-}
      SLM_API_KEY: ${SLM_API_KEY:-}
      MCP_SERVICE_URL: http://mcp-service:3004
      MCP_TRANSPORT: http
      BANKING_SERVICE_URL: http://banking-service:3005
      NLU_SERVICE_URL: http://nlu-service:3003
      CHAT_BACKEND_URL: http://chat-backend:3006
      ALLOWED_ORIGINS: http://localhost:3000,http://localhost:8081
    ports:
      - "3007:3007"
    depends_on:
      postgres:
        condition: service_healthy
      mcp-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3007/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)}).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  chat-backend:
    build:
      context: ../services/chat-backend
    environment:
      NODE_ENV: development
      PORT: 3006
      JWT_SECRET: dev-jwt-secret-change-in-production
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: poc_chat
      DB_USER: poc_user
      DB_PASSWORD: poc_password
      DB_SSL: "false"
      CHAT_PERSISTENCE_ENABLED: "true"
      API_GATEWAY_URL: http://api-gateway:3001
      BANKING_SERVICE_URL: http://banking-service:3005
      NLP_SERVICE_URL: http://nlu-service:3003
      NLU_SERVICE_URL: http://nlu-service:3003
      MCP_SERVICE_URL: http://mcp-service:3004
      AI_ORCHESTRATOR_URL: http://ai-orchestrator:3007
      ALLOWED_ORIGINS: http://localhost:3000,http://localhost:3006,http://localhost:8081
      REDIS_URL: redis://redis:6379
      AGENT_FALLBACK_ENABLED: "true"
    ports:
      - "3006:3006"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      banking-service:
        condition: service_healthy
      nlu-service:
        condition: service_healthy
      mcp-service:
        condition: service_healthy
      ai-orchestrator:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3006/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

  api-gateway:
    build:
      context: ../services/api-gateway
    environment:
      NODE_ENV: development
      PORT: 3001
      JWT_SECRET: dev-jwt-secret-change-in-production
      ALLOWED_ORIGINS: http://localhost:3000,http://localhost:8081
      BANKING_SERVICE_URL: http://banking-service:3005
      NLP_SERVICE_URL: http://nlu-service:3003
      NLU_SERVICE_URL: http://nlu-service:3003
      MCP_SERVICE_URL: http://mcp-service:3004
      CHAT_BACKEND_URL: http://chat-backend:3006
      AI_ORCHESTRATOR_URL: http://ai-orchestrator:3007
      REDIS_URL: redis://redis:6379
    ports:
      - "3001:3001"
    depends_on:
      redis:
        condition: service_healthy
      banking-service:
        condition: service_healthy
      nlu-service:
        condition: service_healthy
      mcp-service:
        condition: service_healthy
      chat-backend:
        condition: service_healthy
      ai-orchestrator:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)}).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  agent-ui:
    build:
      context: ../services/agent-ui
    environment:
      NODE_ENV: development
      PORT: 8081
      REDIS_HOST: redis
      REDIS_PORT: 6379
      API_GATEWAY_URL: http://api-gateway:3001
      CHAT_BACKEND_URL: http://chat-backend:3006
      BANKING_SERVICE_URL: http://banking-service:3005
      NLU_SERVICE_URL: http://nlu-service:3003
      MCP_SERVICE_URL: http://mcp-service:3004
      JWT_SECRET: dev-jwt-secret-change-in-production
      SESSION_SECRET: dev-session-secret-change-in-production
      CORS_ORIGIN: http://localhost:8081,http://localhost:3000
    ports:
      - "8081:8081"
    depends_on:
      redis:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
      chat-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8081/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)}).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  frontend:
    build:
      context: ../services/frontend
      target: development
    environment:
      PORT: 3000
      VITE_API_BASE_URL: http://localhost:3001/api
      VITE_BANKING_SERVICE_URL: http://localhost:3005/api/v1
      VITE_APP_NAME: POC Banking Chat
      VITE_APP_VERSION: 2.0.0
    ports:
      - "3000:3000"
    depends_on:
      chat-backend:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
      banking-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
